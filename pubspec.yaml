name: acme_tasks
description: Offline-first task manager with sync (Flutter)
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.7.0
  sqflite: ^2.3.3
  path_provider: ^2.1.4
  path: ^1.9.0
  connectivity_plus: ^6.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mocktail: ^1.0.3
  lints: ^5.0.0

flutter:
  uses-material-design: true


